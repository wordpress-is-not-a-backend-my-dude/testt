export interface DApp {
  id: string;
  name: string;
  description: string;
  category: string;
  icon: string;
  url: string;
}

export interface Category {
  id: string;
  name: string;
  icon: string;
}